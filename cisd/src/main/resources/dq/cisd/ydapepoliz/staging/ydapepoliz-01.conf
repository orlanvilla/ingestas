hammurabi {
    dataFrameInfo {
        cutoffDate=${?YEAR}"-"${?MONTH}"-"${?DAY}
        frequencyRuleExecution="Daily"
        objectPathName="/in/staging/ratransmit/aplus/cpey/CCISD_D02_"${?YEAR}${?MONTH}${?DAY}"_ydapepoliz_inf_per_poliza.csv"
        physicalObjectName="t_cpey_ydapepoliz_inf_per_poliza"
        submitterId=CO
        uuaa=cpey
    }
    input {
        options {
            delimiter="\t"
            header=true
            mode=FAILFAST
        }
        paths=[
            "/in/staging/ratransmit/aplus/cpey/CCISD_D02_"${?YEAR}${?MONTH}${?DAY}"_ydapepoliz_inf_per_poliza.csv"
        ]
        schema {
            path="${repository.host}/artifactory/${schemas.repo}/schemas/co/cisd/raw/ydapepoliz/latest/ydapepoliz.input.schema"
        }
        type=csv
    }
    rules=[
        {
            class="com.datio.hammurabi.rules.completeness.CompletenessRule"
            config {
                acceptanceMin=100.0
                isCritical=false
                minThreshold=100.0
                targetThreshold=100.0
                withRefusals=false
            }
        }
    ]
}
